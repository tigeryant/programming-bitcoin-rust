/*
use ch_2_elliptic_curves::*;

#[test]
fn creates_valid_point() {
    let _point = Point::new(-1, -1, 5, 7);
}

#[test]
#[should_panic]
fn creates_invalid_point() {
    let _invalid_point = Point::new(-1, -2, 5, 7);
}

#[test]
fn a_equals_b() {
    let point_a = Point::new(-1, -1, 5, 7);
    let point_b = Point::new(-1, -1, 5, 7);
    assert!(&point_a == &point_b);
}

#[test]
fn a_not_equals_b() {
    let point_a = Point::new(-1, -1, 5, 7);
    let point_b = Point::new(18, 77, 5, 7);
    assert!(&point_a != &point_b);
}

*/
