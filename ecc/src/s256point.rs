/*
// implement this? - it may be the better way to model this
use crate::point::Point;
use primitive_types::U256;

#[derive(Debug)]
pub struct S256Point;

impl S256Point {
    // returns a point on the secp256k1 curve
    pub fn new_s256_point() -> Point {
        // x
        Point::new()
    }
}

// A = 0
// B = 7

 */
